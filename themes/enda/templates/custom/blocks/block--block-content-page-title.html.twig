   {% set bg_url = file_url(content.field_image[0]['#media'].field_media_image.entity.uri.value) %}

   <div class="row justify-content-center align-items-center py-5 position-relative" style="background-image: url('{{ bg_url }}'); background-attachment: fixed; background-size: cover;">
       <div class="bg-overlay"></div>
       <div class="col text-center my-5">
           <h1 class="text-white">{{ content.field_title}}</h1>
           <p class="text-white">{{ content.body }}</p>
       </div>

   </div>
